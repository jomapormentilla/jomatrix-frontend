import React from 'react'

const styles = {
    display: 'flex',
    padding: '10px',
    width: '100%',
    border: 'solid 1px'
}

class ProfileMain extends React.Component {
    render(){
        return(
            <div className="profileMain" style={ styles }>
                ProfileMain
            </div>
        )
    }
}

export default ProfileMain